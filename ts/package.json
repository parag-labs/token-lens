{
  "name": "token-lens",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "LLM cost attribution: price books, spend aggregation, anomaly and creep detection",
  "license": "MIT",
  "main": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
